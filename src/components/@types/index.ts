export interface User {
    id: number,
    nome: string,
    registro: number,
    endereco: string,
    email: string,
    telefone: number
}

export interface Providers {
    id: number,
    nome: string, //razão social
    registro: number,
    endereco: string,
    email: string,
    telefone: number
}